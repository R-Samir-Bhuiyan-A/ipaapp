name: bitrisesample
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  http: ^0.13.6 # Optional: Add this if you're calling APIs

  # Example of using a Git package (remove if unused)
  package:
    git:
      url: https://github.com/bitrise-io/sample-apps-flutter-ios-android-package.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
